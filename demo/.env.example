# Same-origin via Vite proxy in dev. Set to full URL in production.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional local-only preview mode. E2E tests force this to false.
VITE_PREVIEW_AUTH_BYPASS=false
