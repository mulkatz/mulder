interface:
  display_name: "Auto Pilot"
  short_description: "Orchestrate Mulder roadmap delivery."
  default_prompt: "Use $auto-pilot to orchestrate the full Mulder roadmap-step workflow from spec creation through verification, review, and finalization."

policy:
  allow_implicit_invocation: false
