interface:
  display_name: "Verify"
  short_description: "Launch Mulder's verification worker."
  default_prompt: "Use $verify to launch the Mulder verification worker for a spec and return the black-box QA verdict with failure evidence."

policy:
  allow_implicit_invocation: false
