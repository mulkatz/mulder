interface:
  display_name: "Review"
  short_description: "Launch Mulder's review worker."
  default_prompt: "Use $review to launch the Mulder architect-review worker for a PR, branch, or spec and return the review verdict."

policy:
  allow_implicit_invocation: false
