interface:
  display_name: "Architect"
  short_description: "Create Mulder specs from roadmap steps."
  default_prompt: "Use $architect to turn the next eligible Mulder roadmap step into an implementation-ready spec, linked issue, and roadmap update."

policy:
  allow_implicit_invocation: false
